name: CI
on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
defaults:
    run:
        shell: bash

jobs:
    test:
        name: Foo bar
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4
            - run: env
            - run: uname -a
            - run: echo ${{ github.event_name }}
